server:
  port: 8082

spring:
  application:
    name: book-service
  datasource:
    url: jdbc:mysql://mysql-book:3306/db_book
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
